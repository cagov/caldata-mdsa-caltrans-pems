version: 2

models:
  - name: stg_pems__station_meta
    description: reformating table columns
    config:
      materialized: table
    columns:
      - name: META_DATE
        description: date
      - name: ID
        description: "station ID"
        tests:
          - not_null
          - unique
