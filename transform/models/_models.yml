version: 2

models:
  - name: stg_pems__station_meta
    description: Staging model form PeMS meta data
    config:
      materialized: table
    columns:
      - name: ID
        description: Station ID
      - tests:
        - not_null
        - unique
        
