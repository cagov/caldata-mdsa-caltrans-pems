version: 2

models:
  - name: stg_pems__station_meta
    description: Staging model for PeMS station metadata
    config:
      materialized: table
    columns:
      - name: ID
        description: "The station ID"
        # tests:
          # - not_null
          # - unique

  - name: int_pems__stations_per_county_counted
    description: intermediate model to count the stations for each county for 2023 data
    config:
      materialized: table
      schema: geo_summary
    columns:
      - name: ID
        description: "The station ID"
        # tests:
          # - not_null
          # - unique

      - name: Station_counts
        description: "The station counts"
        # tests:
          # - not_null
